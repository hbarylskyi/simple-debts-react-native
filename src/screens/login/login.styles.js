import { StyleSheet } from "react-native";

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
    backgroundColor: "#5772AC"
  },

  title: {
    fontSize: 30,
    color: "white"
  },

  loginButtonContainer: {
    marginTop: 100
  }
});
