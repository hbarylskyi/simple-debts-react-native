import React from 'react';
import PropTypes from 'prop-types';
import * as colors from '../../colors';
import OperationBase from '../OperationBase/OperationBase';

const Entity = ({}) => <OperationBase />;

export default Entity;
